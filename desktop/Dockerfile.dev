FROM node:20-alpine

# Install Rust for Tauri
RUN apk add --no-cache curl gcc g++ make python3 git libx11-dev \
 && curl https://sh.rustup.rs -sSf | sh -s -- -y
ENV PATH="/root/.cargo/bin:${PATH}"

WORKDIR /app

COPY . .

RUN npm ci

EXPOSE 3000

CMD ["npm", "run", "dev"]

